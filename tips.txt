Don't preprocess before split
First split then preprocess
Otherwise data leakage

Code doesn't need to work exactly, just try to make it work.

.pth files to save the weights (only usable for a given architecture)

Good practice:
- Training script in other file
- One script: architecture + predictor
- Parameters as arguments
- All parameters + hyperparameters in a config class
- Save best model so far in case something goes wrong
- Early stopping
- Arrange data consistent with real data analysis